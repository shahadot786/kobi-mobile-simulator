included:
  - Kobi

excluded:
  - .build
  - DerivedData
  - Kobi.xcodeproj

opt_in_rules:
  - empty_count
  - closure_spacing
  - force_unwrapping
  - redundant_type_annotation

disabled_rules:
  - todo

line_length:
  warning: 120
  error: 160

identifier_name:
  min_length: 2
  excluded:
    - id
    - x
    - y
